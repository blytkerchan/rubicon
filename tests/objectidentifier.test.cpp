#include "catch.hpp"
#include "../runtime/objectidentifier.hpp"

using namespace Vlinder::Rubicon;

TEST_CASE( "Default-constructed BitStrings are empty", "[objectidentifier]" ) {
    ObjectIdentifier oid;

    REQUIRE(oid.empty());
    REQUIRE(oid.size() == 0);
    REQUIRE(oid.begin() == oid.end());
}
SCENARIO( "when constructed by code as generated by the generator", "[objectidentifier]" )
{
	GIVEN( "an ObjectIdentifier with some value" )
	{

	}
}

